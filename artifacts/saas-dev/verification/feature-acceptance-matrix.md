# Feature Acceptance Matrix

- Gate-ready: yes

| Feature                                                | Acceptance criteria                                                                                         | Verification command/manual check                                                                                                                          | Result | Evidence                                                                                                                                             |
| ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| Upload-first Image Size Checker                        | Initial page shows upload panel only, with no Requirement selector.                                         | `agent-browser snapshot -i` on `/` before upload.                                                                                                          | Passed | Snapshot showed heading `Upload images to check every common size target` and `Choose images`; no Requirement control.                               |
| Results replace upload after upload                    | After selecting files, the upload component disappears and results are shown directly.                      | Uploaded two SVG fixtures with `agent-browser upload @e20 ...`; inspected interactive snapshot.                                                            | Passed | Snapshot showed `Results`, `New check`, batch image buttons, preset tables; no `Choose images` or `Add more` upload control in checker result state. |
| Requirement-aware preset comparison without Fail label | Results list all Web/SEO, Social, and Print presets with status/delta/guidance instead of a confusing Fail. | `agent-browser snapshot -c -d 6` after checker upload.                                                                                                     | Passed | Evidence includes Web/SEO, Social, Print tables with statuses like `Resize and crop` and `Too small for sharp print`; no `Fail` label.               |
| Batch upload and summary                               | Multiple files can be uploaded and selected from a batch summary.                                           | Uploaded `checker-wide-800x400.svg` and `checker-square-512.svg`.                                                                                          | Passed | Snapshot showed two batch buttons with dimensions and file sizes.                                                                                    |
| Image metadata and print calculations                  | Selected image shows preview, file metadata, aspect ratio, total pixels, and PPI print table.               | `agent-browser snapshot -c -d 6` after checker upload.                                                                                                     | Passed | Snapshot showed preview, `Image information`, dimensions `800 x 400 px`, aspect ratio `2:1`, total pixels `320,000`, and PPI rows.                   |
| Image Resizer page                                     | Separate `/resizer` page accepts uploads and exposes preset resize controls.                                | `agent-browser open /resizer`, upload fixtures, snapshot.                                                                                                  | Passed | Snapshot showed preset selector, width/height, fit mode, format, quality, and generate actions.                                                      |
| Manual crop                                            | Manual crop mode enables editable X/Y/crop width/crop height and center crop.                               | Selected fit mode `manual` with agent-browser.                                                                                                             | Passed | Snapshot showed manual crop fields enabled and `Center crop` enabled.                                                                                |
| Batch resize and ZIP download                          | Resize all outputs are generated and Download all ZIP downloads a valid archive.                            | `agent-browser click Resize all`; `agent-browser download @e24 ...`; `unzip -l`.                                                                           | Passed | ZIP `image-size-checker-resized-clean.zip` contains two JPG files sized from the two fixtures.                                                       |
| Mobile responsiveness                                  | Checker upload/results remain readable on 390px viewport.                                                   | `agent-browser set viewport 390 844`; uploaded fixture; captured screenshots.                                                                              | Passed | Screenshots saved under `artifacts/saas-dev/verification/screenshots/checker-mobile-upload.png` and `checker-mobile-results.png`.                    |
| SEO homepage requirements                              | Homepage has at least 1000 visible words and 3%-5% primary keyword density; key pages have metadata.        | `python3 ~/.codex/skills/saas-dev/scripts/validate_seo.py --url http://127.0.0.1:3100/ --url http://127.0.0.1:3100/resizer --keyword "Image Size Checker"` | Passed | SEO checker: 2/2 pages passed; landing visible word count 1072; keyword density 3.0784%; title/description/H1/canonical present.                     |
